{% comment %}
  The chronology axis strip stands in for the site footer on every page:
  a published-figure x-axis (ink hairline + ruler ticks) drawn purely in CSS
  from _sass/_chronology.scss. No copyright bar. The generative curve field
  that sits above it is home-only — its canvas + grain markup and the
  chronology-field.js loader live in _layouts/about.liquid.
{% endcomment %}
<div id="chronology-axis" aria-hidden="true"></div>
